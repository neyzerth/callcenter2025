namespace CallCenter.Server;

public class Server
{
    // public bool WriteFileInPath(IFormFile file, string path)
    // {
    //     // Guardar el archivo en el disco
    //     using (var stream = new FileStream(filePath, FileMode.Create))
    //     {
    //         file.CopyToAsync(stream);
    //     }
    // }
}